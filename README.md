# dist-file-retrieval-engine
A multi-threaded client-server system that enables concurrent document indexing and search across distributed clients. Built with Java socket programming, featuring a dispatcher-worker thread pool pattern and thread-safe inverted index. Achieved 22.45 MB/s indexing throughput processing thousands of documents in parallel.
